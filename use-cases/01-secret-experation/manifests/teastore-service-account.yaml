apiVersion: v1
kind: ServiceAccount
metadata:
  name: teastore-sa
  namespace: teastore
  annotations:
    azure.workload.identity/client-id: "<YOUR_MANAGED_IDENTITY_CLIENT_ID>"
  labels:
    azure.workload.identity/use: "true"
